# Backend API URL (Railway deployment)
# Set this in each Vercel project's Environment Variables dashboard.
# Both frontend domains point to the SAME Railway backend URL.
# Leave unset for local dev — Vite proxy handles /api automatically.
VITE_API_URL=https://your-backend.up.railway.app
